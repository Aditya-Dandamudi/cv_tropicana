# Core dependencies
streamlit
Pillow
ultralytics
opencv-python
numpy

# Used for base64 image encoding
# (standard lib, so not required unless you're on Streamlit Cloud)
# base64 is part of standard Python

# OS and tempfile modules are part of the Python Standard Library, no need to list

# Optional: Pin versions if you want to prevent future breakage
# Example:
# streamlit==1.35.0
# ultralytics==8.1.37
